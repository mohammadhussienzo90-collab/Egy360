# FORCE DEPLOY Dec 23 2024 - Security, caching, transportation, contact fixes
[build]
builder = "nixpacks"

[deploy]
startCommand = "chmod +x startup.sh && ./startup.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
