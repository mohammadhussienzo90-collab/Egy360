# FORCE DEPLOY Dec 21 2024 - Affiliate URLs and SEO blog posts
[build]
builder = "nixpacks"

[deploy]
startCommand = "chmod +x startup.sh && ./startup.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
