# FORCE DEPLOY Dec 11 2025 20:54 - Blog content population CRITICAL FIX
[build]
builder = "nixpacks"

[deploy]
startCommand = "chmod +x startup.sh && ./startup.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
