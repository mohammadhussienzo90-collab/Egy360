# FORCE DEPLOY Dec 24 2024 - Fix missing {% load static %} in base.html
[build]
builder = "nixpacks"

[deploy]
startCommand = "chmod +x startup.sh && ./startup.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
